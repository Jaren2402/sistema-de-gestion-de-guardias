from database import engine; from sqlmodel import Session; from services import buscar_candidatos_sustitucion; s=Session(engine); r1=buscar_candidatos_sustitucion(1, s); print('D?a 1:'); [print(c['estado'], c['nombre']) for c in r1.get('candidatos', [])[:5]]; print('---'); r2=buscar_candidatos_sustitucion(120, s); print('D?a 30:'); [print(c['estado'], c['nombre']) for c in r2.get('candidatos', [])[:5]]
